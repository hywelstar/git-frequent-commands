# Git 安装指南

## Ubuntu 系统上安装 Git

### 方法一：使用 apt 命令安装（推荐）

1. **更新包列表**：

   ```bash
      sudo apt update
   ```

2. **安装**

    ```bash
    sudo apt install git
    ```

3. **验证安装**:

    ```bash 
    git --version
    ```

